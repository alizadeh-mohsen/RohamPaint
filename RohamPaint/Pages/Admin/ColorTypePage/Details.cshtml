@page
@model RohamPaint.Pages.ColorTypePage.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>ColorType</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ColorType.Type)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ColorType.Type)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-primary" asp-page="./Edit" asp-route-id="@Model.ColorType.Id">Edit</a>
    <a class="btn btn-secondary" asp-page="./Index">Back to List</a>
</div>